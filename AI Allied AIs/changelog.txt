1.01:
- Code improvement, less lag: Checking for reputation between AIs is now only done every 10 seconds instead every few miliseconds.
1.02:
- added LighthouseFix, so the mod still works when using eg. "Ruins on Takeover" Mod, by removing the AI lighthouse if it is owned by the player.
